# GFANC FxNLMs 对抗性审查报告

> **审查日期**: 2026-07-30  
> **审查范围**: 架构设计 / 算法 / 可维护性 / 可移植性 / 可量产性 / 实时性  
> **审查方式**: 纯代码审查（未阅读 docs/ 下任何文件）  
> **项目目标**: 窗户开口处主动降噪（声压→0），噪声类型：马路/铁路/儿童玩耍  
> **硬件配置**: 1参考麦 + 3误差麦 + 2扬声器（三误差一参考两扬声器）

---

## 一、架构设计审查

### 1.1 整体架构评价

系统采用经典的双环路 ANC 架构：

```
慢环路 (1Hz, 主线程):  带通→CNN场景分类→Blend 15子滤波器→Wc构造→CrossFader
快环路 (16kHz, 回调):  FxNLMS逐样本自适应 + 反馈抵消 + 啸叫检测/陷波
```

**优点**：
- 快慢环路分离是正确的设计选择——场景识别不需要 16kHz 更新速率
- CNN 双缓冲 + 原子交接实现零样本丢失的跨线程通信
- 场景切换引入 CrossFader（100ms 线性过渡）避免滤波器切换的咔哒声
- Wc 记忆机制（scene_wc[]）保存已收敛滤波器，切回时直接恢复

**关键架构问题**：

#### A1. 【严重】前馈 ANC 的因果性约束在窗户场景下可能被突破

这是本系统最根本的架构风险。前馈 ANC 要求：

> 声学延迟(参考麦→误差麦) > 电子延迟(ADC+DSP+DAC) + 声学延迟(扬声器→误差麦)

对于 50cm 窗户开口：
- 参考麦→误差麦声学延迟：~1.5ms（50cm / 343m/s）
- ADC 缓冲延迟（96样本@48kHz）：2ms
- 带通 FIR 群延迟（512 样本@16kHz）：32ms
- Wc 滤波器群延迟（取决于 Wc 系数）：最大 ~32ms（512 样本@16kHz）
- 扬声器→误差麦声学延迟：~0.15ms（5cm / 343m/s）

**电子延迟总计约 50-66ms >> 声学传播延迟 1.5ms**

这意味着对于频率 > ~15Hz 的非周期信号，反噪声将在噪声抵达误差麦之后才到达，**因果性条件被严重违反**。

*缓解因素*：FxFxLMS 中 LMS 自适应可以学习具有最小群延迟的 Wc；带通 FIR 延迟对参考路径和滤波参考路径一致，可被 Wc 吸收；对于窄带/周期性噪声（如铁路、固定转速引擎），延迟≠相位错误——可以用周期补偿。

但这对**随机宽带噪声**（道路轮胎噪声、儿童玩耍的瞬态声）是一个根本性限制。实测 4-9dB NR（稳态噪声）vs 离线 15dB 的差距，可能相当部分来自因果性约束。

#### A2. 【严重】2扬声器+3误差麦的空间控制能力不足

窗户开口降噪本质上是**有源声屏障**问题，而非耳机/管道 ANC（一维问题）。

- 在 1000Hz（λ=34cm），Nyquist 空间采样要求误差麦间距 < λ/2 = 17cm
- 2 个扬声器的空间 Nyquist 频率对应控制源间距/2。若扬声器间距 30cm，则 >570Hz 出现空间混叠
- "静音区"（zone of quiet）在误差麦周围约 λ/10 范围。500Hz 时 ~7cm，1500Hz 时 ~2.3cm
- **用户耳朵不在误差麦位置**。距离误差麦 30-50cm 处的降噪效果会大幅衰减。

实际场景中，窗户开口是一个二维平面，声波通过开口衍射进入室内。2 个扬声器只能控制该平面上的 2 个"模态"，对于宽频带空间声场控制远远不够。

#### A3. 【中等】反馈抵消 FIR 长度（256tap=16ms）可能不足

房间混响时间（RT60）通常为 0.3-0.8 秒。256tap@16kHz = 16ms 仅能捕获直达声和早期反射，无法覆盖后期混响。残余反馈进入参考信号路径形成 IIR 反馈环路，导致：
- LMS 梯度估计偏差（biased gradient）
- 自适应滤波器 Wc 偏离 Wiener 最优解
- 极端情况下触发啸叫

#### A4. 【中等】次级路径 Ŝ 的静态建模

系统使用预测量/训练得到的固定 Ŝ（从 `secondary_path.bin` 加载），运行时不做任何在线更新。而实际 Ŝ 会因以下因素变化：
- 温度：声速变化 ~0.6m/s/°C，50cm 路径的相位变化在 1000Hz 可达 ~3°/°C
- 窗户开合程度：改变声学边界条件
- 室内物体/人员位置：改变声反射路径
- 扬声器和麦克风老化

**FxFxLMS 的稳定性条件要求 Ŝ 模型相位误差 < 90°**。温度变化 30°C 在 1500Hz 产生约 137° 相位误差（50cm 路径），理论上可突破稳定边界。这是量产部署后的核心风险。

---

## 二、算法审查

### 2.1 FxNLMS 核心算法

**优点**：
- MIMO 结构正确处理了 3 误差麦 × 2 扬声器的耦合
- R-12 环形缓冲优化消除了每样本 7168 次 memmove
- 双段线性访问（零取模）有利于编译器向量化
- R-48 功率 floor 修复（/=E*L 之后才加 floor）消除了安静期步长膨胀
- anti-windup 机制（饱和时冻结梯度 + 200× leak 快速衰减）

**关键算法问题**：

#### B1. 【严重】实时路径 anti_out 不经过 Ŝ 滤波，但梯度却用 Ŝ 滤波参考

这是 FxLMS 的标准做法（输出不滤波，梯度用滤波参考），在 Ŝ 模型准确时是正确的。但本项目的"诚实 NR"计算使用 `anti_est = Wc ⊗ Xd`（Xd 是 Ŝ⊗ref 后的数据），这在 Ŝ 域中评估，与实际物理场可能有偏差。`anti_est/anti_rms` 比值超过 20× 的警告（代码 L523）表明这一偏差在实测中已出现。

#### B2. 【中等】归一化 LMS 的功率计算跨扬声器共享

```c
power[s] = 0;
for (int e = 0; e < E; e++) {
    // sum over all E*L entries for speaker s
}
power[s] = power[s] / (float)(E * L) + 1e-6f;
```

每个扬声器的功率归一化因子是所有误差通道对应滤波参考的均值。这假设各误差通道的滤波参考能量相似。如果某个误差麦的次级路径明显更强/更弱，则归一化会偏向该通道，导致其他通道的适应速度减慢。

#### B3. 【低】泄漏因子 leak=5e-6 对所有系数均匀施加

泄漏会将 Wc 持续向零压缩，本质上是一个正则化项，防止系数漂移。但它也会引入稳态偏差——即使已收敛到最优解，泄漏也会造成残余误差。对于需要大系数幅度的低频段，泄漏导致的性能损失更显著。5e-6 的泄漏在安静段 10 秒内造成约 0.05% 的衰减，影响可忽略；但在 freeze 期无法自适应时（可能持续 >60s），累计衰减约 0.3%，开始可测量。

### 2.2 CNN 场景分类器

**优点**：
- M5 架构（4 ResBlock + Global Average Pool）轻量且有效
- 运行时从 binary 文件推导 K（场景数），灵活适配不同训练结果
- 软判决（softmax 概率加权混合而非硬 argmax）使得场景边界平滑
- 低信号门禁（denom ≤ 0.01 时保持上次分类）防止深夜底噪误分类

**关键算法问题**：

#### B4. 【严重】Min-Max 归一化破坏绝对电平信息

```c
float denom = mx - mn;
for (int i = 0; i < 16000; i++) cnn_in[i] = audio[i] / denom;
```

所有 1 秒窗口被归一化到 [-1, 1] 范围（取决于峰峰值）。这意味着：
- 低声压级的深夜道路噪声（如 40dBA）和高声压级的白天道路噪声（70dBA）经过归一化后看起来完全一样
- CNN 只能识别"频谱形状"而无法区分"绝对声压级"
- 然而 FxNLMS 的最优 Wc 可能依赖于绝对电平（非线性扬声器、不同声压级的混响特性不同）

#### B5. 【中等】仅 3 类场景（K=3）

K=3 场景需要覆盖：马路噪声、铁路噪声、儿童玩耍噪声——以及它们的任意混合。3 个 centroid 的凸组合（softmax 混合）可以表示 simplex 内的任意点，但表达能力受限于 3 个基向量张成的子空间。对于"铁路+儿童"或"马路+铁路"等混合场景，混合系数可能不精确。

更重要的是，**同一类型噪声在不同声学环境下的最优 Wc 可能不同**（如雨天道路噪声 vs 干燥道路噪声），CNN 可能无法区分这些子类。

#### B6. 【低】CNN 每秒推理一次，1 秒窗口的时间分辨率

突发噪声（如儿童尖叫、汽车鸣笛）的起始时间可能在 100-300ms 量级。1 秒的 CNN 窗口意味着最坏情况下需要 1 秒才能检测到噪声类型变化并切换 Wc，加上 CrossFader 100ms 过渡和 LMS 重新收敛（可能 10-30 秒），系统对突发噪声的响应很慢。

### 2.3 啸叫检测与陷波

**优点**：
- 双通道检测（err + anti）避免限幅后的梳状谐波误检
- 滞回状态机（4 帧确认 + 8 帧释放 + 512ms 最小保持）
- 每扬声器独立 IIR 状态避免跨通道串扰

**关键算法问题**：

#### B7. 【中等】DFT 频率分辨率过粗

DFT 使用 256 点 FFT（实际是逐个 bin 的 DFT），频率分辨率 = 16000/256 = 62.5Hz。这意味着：
- 实际啸叫频率 1000Hz 被量化为 1000Hz（bin 16）
- IIR 陷波器带宽（r=0.96）在 1000Hz 约 80Hz（-3dB 带宽），会同时衰减目标频率和相邻 ~80Hz 的噪声分量
- 如果有两个啸叫频率落在同一 bin 内，系统只能抑制其中一个

#### B8. 【低】最多 2 个陷波器可能不够

在高增益、小空间的声反馈系统中，可能同时存在多个啸叫频率（基频+谐波）。`HW_MAX_NOTCHES=2` 意味着第三个啸叫频率无法被抑制。

### 2.4 反馈抵消

#### B9. 【中等】NLMS 校准使用白噪声而非宽带伪随机序列

`calibrate_feedback.c` 使用 `rand()` 生成白噪声（均匀分布）。MLS（Maximum Length Sequence）或对数扫频信号在系统辨识中具有更好的信噪比和更平坦的频谱。4 秒的白噪声激励对于 256tap FIR 辨识在统计上是足够的（64000 样本，样本/系数比 = 250:1），但在低频段信噪比可能不足。

---

## 三、可维护性审查

### 3.1 代码组织

**优点**：
- 模块化头文件/源文件分离清晰
- 集中参数结构体 `gfanc_config_t` 统一管理可调参数
- 分级日志宏（LOG_ERROR/WARN/INFO/DEBUG）
- 注释中带有问题追踪编号（R-xx, P-x, CR-x, S-x）

**关键问题**：

#### C1. 【中等】`main.c` 和 `main_realtime.c` 之间有大量逻辑重复

场景管理状态机、Wc 发散检测、收敛判定逻辑在离线版和实时版中各实现了一遍。修改一个版本时另一个版本容易遗漏。

#### C2. 【中等】CNN 模块使用全局静态变量

```c
static cnn_m5_t g_cnn;
static int g_K = 0;
static float *g_cnn_buf = NULL;
```

这使 CNN 模块不可重入——无法在同一进程中运行两个独立的 ANC 实例（如多窗户场景）。`static int b_len` 的持久化存在同样的重入问题。

#### C3. 【低】magic number 散布在代码中

- Wc 初始 RMS 目标 `0.03f` 直接硬编码在 `main_realtime.c:854`
- `250` 样本的 anti_est 计算窗口（main_realtime.c:340）
- `4e-5f` 的 mute 期 Wc 衰减率（main_realtime.c:264）
- `0.004f` 的 out_gain slew rate（main_realtime.c:398）

这些值应纳入 `gfanc_config_t` 或至少以 `#define` 集中管理。

#### C4. 【低】测试覆盖率极低

测试套件仅包含：
- FIR 脉冲响应正确性（`test_fir.c`）
- FxNLMS 在 δ 路径下的理想收敛（`test_fir.c`）
- 黄金回归（SHA256 对比）

**缺少以下关键测试**：
- 发散保护触发与恢复
- 场景切换 + CrossFader 完整性
- 啸叫检测的 ROC 特性（检测率 vs 误报率）
- 反馈抵消有效性（在线 ANC 运行中 fb_est 是否真的降低 ref 中的反馈）
- 边界条件测试（NaN/Inf 注入、全零输入、最大振幅输入、缓冲区溢出）
- 实时性能回归（回调延迟分布）

---

## 四、可移植性审查

#### D1. 【严重】Windows 深度绑定

- `windows.h` 依赖：`InterlockedExchange`, `InterlockedIncrement`, `Sleep`, `SetConsoleOutputCP`, `LoadLibraryA`, `GetProcAddress`, `CTRL_C_EVENT`
- PortAudio ASIO DLL 运行时加载——ASIO 仅在 Windows 上可用
- WASAPI/WDM-KS 也是 Windows 专用 API

移植到 Linux（ALSA/JACK）或嵌入式 RTOS 需要重写整个音频 I/O 层和线程同步原语。

#### D2. 【中等】`volatile` 和 `Interlocked` 混用

代码混合使用 `volatile` 关键字和 `Interlocked*` 函数进行线程同步：
- `volatile LONG wc_seq` + `InterlockedExchangeAdd` — 正确
- `volatile int active_count` — 仅声明 volatile，无原子操作保护
- `volatile float nr_level` — 对 float 的 volatile 读写不保证原子性（在 32 位平台上 float 读写可能非原子）

这种不一致的同步策略在 ARM 或其他非 x86 平台上可能引入数据竞争。

#### D3. 【中等】`double` 精度 FIR 延迟线

```c
double *delay_line;  // 在 fir_filter_t 中
```

在 `gfanc_types.h` 中 `fir_filter_t` 使用 `double` 精度延迟线，理由是"匹配 Python scipy float64"。这对 PC 平台不是问题，但对于嵌入式 DSP（如 ARM Cortex-M7 单精度 FPU），double 运算需要软件模拟，速度慢 10-100×。

#### D4. 【低】GCC 特定的 `__VA_ARGS__` 扩展

`LOG_ERROR(fmt, ...) fprintf(stderr, "[ERROR] " fmt "\n", ##__VA_ARGS__)` 中的 `##__VA_ARGS__` 是 GCC 扩展，MSVC 不支持。

---

## 五、可量产性审查

#### E1. 【严重】每台设备需要单独校准

反馈路径校准（`calibrate_feedback.exe`）依赖：
1. 用户手动运行校准程序
2. 选择正确的 ASIO 设备
3. 保持房间安静 4+4 秒
4. 校准结果写入 `data/feedback_path_0/1.bin`

量产场景中这一流程完全不可行。需要：
- 出厂前在消声室统一校准（但反馈路径依赖实际安装环境！）
- 或在设备上集成自动在线校准功能（利用运行时噪声作为激励信号做在线系统辨识）

反馈抵消若未正确校准（或声学环境变化后），残余反馈通过参考麦形成正反馈环路，触发啸叫→陷波→降噪效果下降。

#### E2. 【严重】次级路径 Ŝ 离线训练且无法在线更新

`secondary_path.bin` 是在特定硬件上训练的。量产中每个设备的扬声器-误差麦声学路径会因组装公差、麦克风灵敏度差异、扬声器频率响应差异而不同。且 Ŝ 会随温度、湿度、灰尘积累而缓慢变化。

**没有在线 Ŝ 辨识/更新机制**是量产后的最大风险之一——即使出厂时降噪效果良好，半年后可能因声学路径变化而显著退化。

#### E3. 【严重】依赖特定 CNN 模型权重

CNN 模型（58 个 .bin 文件）是在特定硬件配置上训练的。如果供应链中更换麦克风型号、扬声器型号，或声卡规格变化，整个 CNN 模型 + 子滤波器需要重新训练。这一过程的自动化程度和可重复性不明。

#### E4. 【中等】无远程监控/OTA 能力

- `gfanc_log.csv` 仅本地文件追加，无远程上报
- 没有固件/权重 OTA 更新机制
- 没有设备健康自检（麦克风故障、扬声器故障检测）
- 没有运行状态指示灯/用户反馈接口设计

#### E5. 【中等】无降级策略

如果 3 个误差麦中的 1 个故障（如进水损坏），当前代码没有单通道故障检测和降级运行能力。系统会继续使用错误信号驱动 LMS 梯度更新，导致所有通道发散。

#### E6. 【中等】Wc 初始 RMS 基于启发式硬编码

```c
float wc_target = 0.03f;  // 硬编码在 main_realtime.c
```

这个值在 `[0.005, 0.05]` 范围内钳位。它代表反噪声的初始幅度。对于不同扬声器灵敏度、不同房间声学特性，这个值可能需要调整。量产中需要自动标定而非硬编码。

#### E7. 【低】环境变量配置不适合嵌入式部署

所有运行时可调参数通过 `getenv()` 读取（`GFANC_MIC_GAIN`, `GFANC_STEP` 等）。这对开发/调试方便，但量产嵌入式设备没有环境变量概念。需要换成配置文件（JSON/TOML）、硬件拨码开关、或上位机配置接口。

---

## 六、实时性审查

#### F1. 【低】PC 平台计算负荷充足，嵌入式平台存疑

当前代码在 PC 上运行，16kHz 采样率下每样本处理时间预算为 62.5μs。

主要热路径（fxnlms_tick_rt 每样本）：
- 6 个 Ŝ FIR（6×1024=6144 MAC）+ 1 个 BP FIR（1024 MAC）+ 2 个 anti 输出（2×1024=2048 MAC）= ~9216 MAC
- 梯度更新：E×S=6 个通道，每通道 1024 tap = 6144 MAC
- 总计约 15360 MAC/样本 × 16000 = **~246M MAC/秒**

这对现代 x86 CPU 不算什么（<1% CPU），但对嵌入式 DSP（如 ARM Cortex-M7 @ 480MHz）可能占用 30-60% CPU。若需要增加在线 Ŝ 辨识、更多通道等功能，余量不足。

#### F2. 【低】CNN 推理 ~8ms @ 1Hz 在接受范围内

CNN 推理在主线程中执行，不影响音频回调。8ms 的推理时间对于 1Hz 的更新速率完全可接受。但需注意 `scene_ctrl_blend_wc` 中每次调用 `scene_ctrl_construct_wc`（K 次 malloc/free 2048 floats），产生内存碎片风险。高频次 malloc/free 在主线程中可接受但不够优雅。

#### F3. 【信息】音频回调无最坏情况执行时间（WCET）分析

代码中没有回调执行时间测量。PortAudio 回调中若执行时间超过缓冲时长（96 样本@48kHz=2ms），会导致音频丢帧（glitch）。在安全静音/啸叫检测/发散保护同时触发的最坏情况下，执行路径更长。建议增加回调执行时间监控。

---

## 七、最大风险点排名

### 🥇 风险 #1：次级路径 Ŝ 模型误差导致的降噪退化 / 发散

**严重程度**：致命  
**影响**：降噪效果随时间退化，极端情况下系统不稳定（正反馈啸叫）

**根因分析**：
1. FxLMS 算法的 90° 相位裕度条件要求 Ŝ_model 与 S_real 的相位误差在任何频率都 < 90°
2. 当前 Ŝ 是离线静态模型，不随温度/湿度/老化/窗户状态而更新
3. 温度变化 30°C → 50cm 路径 1500Hz 处相位变化约 137° → 突破稳定边界
4. 硬件组装公差导致每台设备的 Ŝ 与标称值有差异
5. 没有在线 Ŝ 辨识或自适应机制

**验证方法**：在恒温箱中测量不同温度（5°C/25°C/45°C）下的 NR，如果不稳定或 NR 随温度显著下降，则确认此风险。

### 🥈 风险 #2：前馈 ANC 因果性约束导致的宽带降噪不足

**严重程度**：致命  
**影响**：对随机宽带噪声（道路轮胎噪声）降噪效果差，仅对窄带/周期性噪声有效

**根因分析**：
1. 参考麦→误差麦的声学传播延迟（~1.5ms for 50cm）小于电子+声学总延迟（~50ms+）
2. 反噪声在噪声之后到达误差麦，违反因果性
3. 系统用 Wc 的"预测"能力弥补延迟——窄带/周期性噪声可预测，随机宽带噪声不可预测
4. 这与窗户 ANC 的物理约束直接冲突：窗户越短（越紧凑的产品），因果性约束越难满足

**验证方法**：
- 在离线仿真中用不同带宽的噪声测试（白噪声 vs 粉红噪声 vs 单音），确认 NR 与带宽的负相关
- 在不同滤波器长度的仿真中测试（L=256/512/1024/2048），短滤波器应有更好的因果性但更差的频率分辨率——找到最优折中

### 🥉 风险 #3：空间欠采样导致的静音区局限

**严重程度**：严重  
**影响**：误差麦处有降噪但人耳位置无降噪或反而增强（局部干涉增强）

**根因分析**：
1. 2 扬声器 + 3 误差麦控制二维窗户开口平面的声场
2. 静音区约为 λ/10（500Hz: 7cm, 1500Hz: 2.3cm）
3. 用户耳朵距离误差麦通常 30-50cm 以上
4. 高频（>800Hz）空间控制几乎不可能用现有硬件实现

**验证方法**：
- 在误差麦后方 10/20/30/50cm 处放置额外测量麦，测量各级距离的实际 NR
- 绘制窗户平面的空间 NR 分布图

### 风险 #4：量产中的逐设备标定问题

**严重程度**：严重  
**影响**：无法批量生产，或出厂 NVH 一致性差

**根因分析**：见 §E1、E2、E3。

### 风险 #5：反馈环路残余导致的 Wc 估计偏差

**严重程度**：中等  
**影响**：降噪效果低于理论最优，且随房间配置变化

**根因分析**：
1. 反馈抵消 FIR（256tap=16ms）无法覆盖房间混响尾部
2. 残余反馈通过参考麦进入参考信号 → LMS 梯度出现偏差 → Wc 偏离 Wiener 解
3. 这是反馈 FxLMS 的已知问题（biased gradient），当前架构未使用 IMC（Internal Model Control）或反馈中性化技术来解决

### 风险 #6：CNN 场景分类器与 FxNLMS 最优滤波器之间的"语义鸿沟"

**严重程度**：中等  
**影响**：CNN 认为是"同一场景"的噪声可能需要截然不同的最优 Wc

**根因分析**：见 §B4、B5。核心问题是 CNN 看到的归一化频谱形状 ≠ FxNLMS 需要的声学传递函数。

---

## 八、降噪效果不理想的最可能原因

如果实测降噪效果不理想（低于预期的 15dB 以上），按概率排序：

1. **因果性约束**（概率最高，~40%）：参考→误差的声学延迟太短，系统无法在噪声到达前产生反噪声。症状：低频（<200Hz）降噪尚可但中高频（500-1500Hz）降噪接近零甚至为负。

2. **Ŝ 模型失配**（~25%）：加载的 secondary_path.bin 与实际硬件声学路径不一致。症状：某些频率反而增强（>0dB），或离线仿真 NR 良好（因为离线也使用同样的 Ŝ）但实时 NR 差。

3. **空间静音区局限**（~20%）：误差麦处有 15dB NR 但人耳位置（远离误差麦）几乎无效果。症状：靠近窗户感觉安静，后退一步噪声恢复甚至更响（声聚焦）。

4. **反馈环路污染参考信号**（~10%）：反馈抵消不充分→残余反馈进入参考路径→LMS 收敛到有偏解。症状：某些频段出现周期性波动（"水声/风声"效果），NR 不稳定。

5. **场景分类错误**（~5%）：CNN 在当前噪声环境下选择了不合适的 Wc 初始值，LMS 无法在合理时间内收敛。症状：场景切换后 NR 明显下降并持续数十秒。

---

## 九、改进建议摘要

| 优先级 | 类别 | 建议 | 工作量 |
|--------|------|------|--------|
| P0 | 架构 | 增加在线 Ŝ 辨识（在环境噪声足够时用 NLMS 更新 Ŝ 模型） | 大 |
| P0 | 架构 | 增加第 3 或第 4 个扬声器提升空间控制能力 | 硬件 |
| P0 | 算法 | 量化分析因果性约束对带宽的限制，明确系统的有效频率上限 | 中 |
| P1 | 量产 | 设计自动出厂标定流程（消声室+标准声源） | 大 |
| P1 | 量产 | 实现麦克风/扬声器故障检测与降级运行 | 中 |
| P2 | 算法 | K 从 3 增加到 5-8，提升场景分辨能力 | 中（需重新训练 CNN） |
| P2 | 算法 | 反馈路径使用在线自适应 FIR（与 ANC 同时运行） | 中 |
| P2 | 可移植 | 将平台相关代码抽象为 HAL 层（Audio, OS, Thread） | 大 |
| P3 | 可维护 | 提取 main.c 和 main_realtime.c 的共享逻辑到 scene_manager.c | 中 |
| P3 | 可维护 | 改善测试覆盖率（发散保护、场景切换、边界条件） | 中 |
| P3 | 量产 | 配置文件替代环境变量 + OTA 更新支持 | 中 |
